spring.application.name=api-gateway
server.port=9090

eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Automatically create routes to all Eureka-registered services,
# e.g. http://localhost:9090/greet-service/greet -> greet-service /greet
spring.cloud.gateway.discovery.locator.enabled=true
spring.cloud.gateway.discovery.locator.lower-case-service-id=true

# Log the LogFilter's INFO messages to the console
logging.level.com.cognizant.apigateway.filter=INFO
